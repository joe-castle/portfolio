// ---------------------------------------------
// ------ Reset
// ---------------------------------------------
@import '~normalize.css/normalize.css';

// Apply a natural box layout model to all elements,
// but allowing components to change
html
  box-sizing border-box

*
*:before
*:after
  box-sizing inherit

// ---------------------------------------------
// ------ Fonts
// ---------------------------------------------
@import url('https://fonts.googleapis.com/css?family=Source+Code+Pro|Source+Sans+Pro:300,700')

// ---------------------------------------------
// ------ Typography
// ---------------------------------------------
$body-font = text-font-stack
$header-font = text-font-stack

$min-font = 16px

$body-color = body-font-color
$header-color = header-font-color

typographic()

grid-overlay(black, 17)

// ---------------------------------------------
// ------ Helpers
// ---------------------------------------------
// Ensures site takes up full height of the page
html
body
#root
.App
  height 100%

// ---------------------------------------------
// ------ Basic Styles
// ---------------------------------------------
a
  color header-font-color
  text-decoration none
  transition color .2s linear

  +hover-state()
    color rgba(@color, .5)
